%h1 Main Page
%p Welcome to the main page of the Image Gallery application.

.container
  .row
    .col-md-12
      .d-flex.justify-content-between.align-items-center.mb-4
        %div
          %h2 Featured Images
          %p.mb-0 Explore our collection of beautiful images.
        - if user_signed_in?
          = link_to new_post_path, class: "btn btn-primary" do
            %i.bi.bi-plus-circle.me-2
            = t('posts.create_new')
      
      - if @posts.present?
        .div.d-flex.justify-content-center{ :style => "font-size: 1rem;" }
          - if @posts.respond_to?(:total_pages)
            = paginate @posts
        .row
          - @posts.each do |post|
            .col-md-4.mb-4
              .card
                - if post.image.present?
                  = image_tag post.image.url, class: 'card-img-top'
                .card-body
                  %h5.card-title= post.title
                  %p.card-text= truncate(post.text, length: 100)
                  = render 'shared/post_categories', post: post
                  .mt-2
                    = link_to 'View Post', post_path(post), class: 'btn btn-sm btn-primary'
      - else
        %p No posts found.                                      